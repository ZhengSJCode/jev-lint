# jev-lint 规则

每条规则一个小节，小节标题是规则的 id：

```markdown
## <id>

- 标题: 出现在报告里的名字
- 范围: 函数（默认）或 文件
- 阈值: 这条规则自己的判定线，省略则用全局的 0.7（可选）
- 提问: 发给 jev 的问题。**措辞即口径** —— 问「是否违反单一职责」拿到的是违规概率，
        问「是否只做一件事」拿到的是合规概率，两者互补但阈值方向相反
- 标准: 随提问一起送去的判定口径。改提问不改标准，两次判定的尺子就不一样了
```

**范围**决定这条规则问几遍：

- `函数`（默认）—— 对文件里每个函数各问一次，state 里放的是那一个函数
- `文件` —— 整个文件只问一次，state 里放的是文件路径和全文

「文件位置是否合理」这类问题只在文件尺度上成立，逐函数问 N 遍既浪费又答不准。

id 是稳定标识，会写进报告，改名等于换了一条规则，历史结果不再可比。

只收「说出来没有争议」的规则。像「命名是否优雅」这种主观题，jev 给的概率在不同文件间
没有可比性，阈值也就无从设起，不如不收。

## srp

- 标题: 违反单一职责
- 阈值: 0.65
- 提问: 这个函数体里是否同时出现了三类以上彼此无关的动作 —— 解析入参、写数据库、发网络请求、写文件、记日志、更新缓存、发消息这类，而且每一类都是它自己在直接做（不是交给已拆好的子函数）？
- 标准: 判据是**数得出几个彼此无关的动作**，不是「感觉它做了很多」。**编排函数不算**：只依次调用几个已经拆好的子功能、自己不直接碰外部系统（如 `register` 里依次 `on(...)`、`main` 里依次调几个 setup），它做的就是编排这一件事。
  （曾经问的是「是否违反单一职责」，实测会把「结构整齐但干了五件事」的函数判到 0.56 —— 模型看的是写得整不整齐，不是数职责。换成能数出来的判据后同一批样本分得开。）

## dead

- 标题: 注释掉的死代码
- 提问: 这个函数里是否留有注释掉的死代码？
- 标准: 注释掉的死代码、调试残留、被注释代替删除的旧实现

## dup

- 标题: 重复代码
- 提问: 这个函数里是否有明显重复的代码块？
- 标准: 同一段逻辑在函数内重复出现，只有参数不同

## flag-arg

- 标题: 布尔参数控制行为
- 提问: 这个函数是否有一个布尔参数，它的取值决定函数走两条完全不同的路？
- 标准: 布尔标志把两个函数硬塞进一个签名 —— 调用处写 `render(data, true)` 时，读者看不出 true 是什么意思，只能进去翻实现。判据：某个参数类型是 boolean，且函数体内有以它为条件的、会走显著不同路径的分支（不是简单地把某个值原样传下去）。

## output-arg

- 标题: 通过参数返回结果
- 提问: 这个函数是否把结果写进传入的参数，而不是自己返回？
- 标准: 判据是数据流向被藏起来了 —— 调用处 `count(items, result)` 看不出 result 会被改写。函数体里有对参数（或其成员）的赋值，而函数自身返回 void 或 undefined，就属于这种。

## magic

- 标题: 魔法数字
- 提问: 这个函数里是否有含义不明的数字字面量，既没有常量名、紧邻处也没有说明它的来历？
- 标准: 判据是「读者说不出这个数字为什么是它」。0 / 1 / -1 这类惯用值、以及紧邻处有注释或常量名解释的，不算。

## side-effect

- 标题: 只读名字下的写副作用
- 提问: 这个函数的名字是否以 get / is / has / calc / format / to / parse 这类**只读或纯计算**的前缀开头，但函数体却修改了外部状态，或产生了写副作用（写库、发请求、改全局、改缓存）？
- 标准: 判据是**前缀**，不是「感觉名字像查询」—— 后者是主观的，实测反向。（曾经写成「名字暗示只读」，反例 `syncUser` 被判 0.88；换成前缀后同一批样本 0.96/0.94 对 0.03/0.02/0.10。）

## stringly-typed

- 标题: 用自由文本判断状态
- 阈值: 0.9
- 提问: 这个函数是否靠比较字符串内容（相等、包含、前后缀、关键词、正则、标点）来判断程序状态、行为语义或控制流程，而不是读取结构化字段（明确的结果字段、类型化事件、状态机状态、枚举值）？
- 标准: 判据是「判断的依据是自由文本」。包括：对消息正文、日志、模型输出做字符串匹配来推断「成功」「失败」「正在提问」这类语义；把猜出来的语义再写回 metadata、事件类型或状态字段。**不算**：协议定义的枚举值、类型判别字段、以及语言层面的正常字符串处理（拼路径、格式化、判别文件扩展名）。

## comment-restates

- 标题: 注释只复述代码
- 提问: 这个函数里的注释是否有只是把代码在做什么又用中文说了一遍的（「遍历数组」「返回结果」「设置变量」），而不是解释**为什么**要这么做、它防的是什么问题？
- 标准: 判据是「删掉这条注释，读者会少知道什么吗」。**不算**：解释约束、踩过的坑、业务规则的注释 —— 那些是代码本身表达不出来的。整段被注释掉的代码也不在这里（那是 `dead`）。

## mixed-return

- 标题: 返回路径不一致
- 提问: 这个函数是否存在某些分支 return 了值，而另一些分支没有 return（隐式返回 undefined）？
- 标准: 所有分支都得能被枚举到，纯结构判断。判据是调用方拿到 undefined 时无从分辨「函数没走到那个分支」还是「正常返回了空」—— 尤其在 TypeScript 里，它会逼出一堆 `as` 或者让类型直接不准。

## new-dep

- 标题: 硬编码具体依赖
- 提问: 这个函数是否在函数体内部直接构造或取得某个具体的协作者（数据库、HTTP 客户端、服务实例），而不是从参数接收它？
- 标准: 判据是「这个依赖换不掉、也替不了，因此这个函数没法被单独测」。

## long-function

- 标题: 函数过长
- 提问: 这个函数是否超过 30 行（含签名与闭合括号）？
- 标准: 数行数，不是感觉。超过 30 行说明它承担了多个职责，先拆职责而不是删空行凑数。**不算**：纯数据声明、长 `switch` 的每个分支只有一行 —— 那类没有可拆的职责。

## over-wrapping

- 标题: 过度封装
- 提问: 这个函数（或它引入的类型）是否为一个简单透传包了一层 —— 为了「以后可能变」而引入了中间对象、包装类型、`kind`/`type` 判别字段，或者把上游的字符串包成对象又原样取出？
- 标准: 判据是「去掉这层封装，调用方是否更短、类型是否更直接」。**不算**：真正统一了多个实现的抽象；接口边界上必须做的形状转换（外部协议 → 内部模型）；给已有类型加语义的 branded type。
## placement

- 标题: 文件位置不合理
- 范围: 文件
- 提问: 这个文件是否被放在了与它职责不符的目录下——比如具体领域的业务逻辑塞进了 utils/ common/ helpers/ 这类按技术分层的目录，或者所在目录的领域与文件内容对不上？
- 标准: 文件应放在与它职责对应的领域或功能目录下（如 order/ payment/ session/）。放在 utils/ common/ helpers/ misc/ 这类**按技术分层**的目录里、而内容又是某个具体领域的逻辑，就是位置不合理；文件名与所在目录的领域不一致也算。

<!--
  曾经有过一条 `swallow`（吞掉异常），已删除。留个记录免得有人再走一遍：

  三轮措辞调整都没让它稳定 —— 对照组里 `catch (e) {}` 这种最典型的空 catch
  判成「不违规」，而 `catch { return {} }`（有交代的降级）反而判成违规。

  根因是它**需要看调用方**：错误去了哪儿、上边有没有兜底，只有整个调用链
  才知道，jev 逐函数看只能猜。这类规则不适合放在这里。
-->
